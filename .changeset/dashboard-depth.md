---
"@jgalego/teamapi-rest-api": minor
---

The dashboard covers the AI-native half of the API it had been ignoring: an agent roster with unowned agents marked, AI session history, the context map's conflicts alongside the diagram, and a walkable knowledge graph. `/health` reports which optional surfaces the server has.
